import Announcement from "@/features/announcment";
import React from "react";

const page = () => {
    return (
        <div>
            <Announcement />
        </div>
    );
};

export default page;
